// tasks.json
{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Jupyter",
            "type": "shell",
            "command": "jupyter notebook --notebook-dir=/workspaces/segmentor --ip 0.0.0.0 --no-browser --allow-root"
        },
    ]
}